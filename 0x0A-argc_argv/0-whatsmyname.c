#include <stdio.h>
#include "main.h"
/**
 * main - return the exefile name
 * Return:always 0
 * @argc: argument 1
 * @argv: argument2
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}
