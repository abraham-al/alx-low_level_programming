#include "main.h"
/**
 * _strlen - returns length of string
 * @s: input parameter
 * Return: always (0)
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{
		continue;
	}
	return (i);
}
